const inputsPrintItems = (a:number, b:number) => {
    for(let i = 0; i < a; i++){
        console.log(i)
    }
    for(let i = 0; i < b; i++){
        console.log(i)
    }
}

// inputsPrintItem are O(a +b)
// That's why you have to use different terms for inputs